spring:
  application:
    name: zhihao
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server-git
      profile: pro
      label: config-label-test
      fail-fast: true

server:
  port: 8080

eureka:
  client:
    service-url:
     defaultZone: http://localhost:8761/eureka
